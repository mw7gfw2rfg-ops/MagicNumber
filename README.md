# MagicNumber
Python program that runs from the terminal. When given files/directory, it will read the binary file header - first 32-bits of file - and compares this to a list of known file headers (magic numbers). Can be used to stop malware being unleashed on devices.
